# 快速入门

<cite>
**本文档中引用的文件**  
- [README.md](file://README.md)
- [installation.md](file://docs/getting-started/installation.md)
- [5-minute-setup.md](file://docs/getting-started/5-minute-setup.md)
- [first-notebook.md](file://docs/getting-started/first-notebook.md)
- [.env.example](file://.env.example)
- [docker-compose.yml](file://setup_guide/docker-compose.yml)
- [docker.env](file://setup_guide/docker.env)
- [Dockerfile](file://Dockerfile)
- [Makefile](file://Makefile)
</cite>

## 目录
1. [简介](#简介)
2. [系统要求](#系统要求)
3. [安装方法](#安装方法)
4. [环境配置](#环境配置)
5. [通过Docker部署](#通过docker部署)
6. [直接运行部署](#直接运行部署)
7. [创建第一个笔记本](#创建第一个笔记本)
8. [常见问题排查](#常见问题排查)
9. [验证系统运行](#验证系统运行)

## 简介

Open Notebook 是一个开源、注重隐私的研究助手，可作为 Google Notebook LM 的替代品。本指南将引导您完成安装和初始配置，帮助您在最短时间内体验核心功能。

本指南整合了 `5-minute-setup.md` 中的快速配置要点和 `installation.md` 中的详细安装说明，确保内容连贯且实用。您将学习如何通过 Docker 或直接运行的方式部署系统，配置环境变量，初始化数据库，并创建您的第一个笔记本。

**核心优势**：
- 🔒 **数据隐私**：您的研究数据完全由您掌控
- 🤖 **多模型支持**：支持 16+ 个 AI 提供商，包括 OpenAI、Anthropic、Ollama 等
- 📚 **多模态内容**：支持 PDF、视频、音频、网页等多种内容类型
- 🎙️ **播客生成**：可生成专业的多说话人播客
- 🔍 **智能搜索**：支持全文和向量搜索

**重要提示**：如果您是从旧版本升级，请注意 Docker 标签已更改，`latest` 标签已冻结在最后一个 Streamlit 版本。新用户请使用 `v1-latest` 标签。

## 系统要求

在开始安装之前，请确保您的系统满足以下要求：

### 硬件要求
- **CPU**：2 核以上（推荐 4 核以上）
- **内存**：至少 4GB（推荐 8GB 以上）
- **存储**：10GB 以上可用空间
- **网络**：稳定的互联网连接（用于访问 AI 模型）

### 操作系统支持
- **macOS**：10.15 (Catalina) 或更高版本
- **Linux**：Ubuntu 18.04+、Debian 9+、CentOS 7+、Fedora 30+
- **Windows**：Windows 10 或更高版本（推荐使用 WSL2）

### 软件先决条件
- **Docker**：最新版本（推荐用于初学者）
- **Python**：3.9 或更高版本（用于源码安装）
- **uv**：Python 包管理器（用于源码安装）

**Section sources**
- [installation.md](file://docs/getting-started/installation.md#system-requirements)

## 安装方法

Open Notebook 支持多种安装方法，您可以根据自己的需求选择最适合的方式。

| 方法 | 适用人群 | 难度 | 优点 | 缺点 |
|------|----------|------|------|------|
| **Docker 单容器** | 初学者、简单部署 | 简单 | 一键设置，环境隔离 | 控制性较差，调试困难 |
| **Docker 多容器** | 生产部署 | 中等 | 可扩展，专业设置 | 配置更复杂 |
| **源码安装** | 开发者、需要定制 | 高级 | 完全控制，易于调试 | 需要 Python 知识 |

**Section sources**
- [installation.md](file://docs/getting-started/installation.md#installation-methods)

## 环境配置

Open Notebook 使用环境变量进行配置。您需要创建一个 `.env` 文件（或 `docker.env` 用于 Docker）来配置您的环境。

### 核心配置

```env
# 安全配置（可选 - 用于公共部署）
OPEN_NOTEBOOK_PASSWORD=your_secure_password_here

# 数据库配置
SURREAL_URL="ws://localhost:8000/rpc"
SURREAL_USER="root"
SURREAL_PASSWORD="root"
SURREAL_NAMESPACE="open_notebook"
SURREAL_DATABASE="production"
```

### AI 提供商配置

#### OpenAI（推荐给初学者）
```env
# 提供：语言模型、嵌入、TTS、STT
OPENAI_API_KEY=sk-your-openai-key-here
```

#### Anthropic（Claude 模型）
```env
# 提供：高质量语言模型
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
```

#### Google（Gemini）
```env
# 提供：大上下文模型、嵌入、TTS
GEMINI_API_KEY=your-gemini-key-here
```

#### Ollama（本地 AI 模型）
```env
OLLAMA_API_BASE="http://localhost:11434"
```

**Section sources**
- [.env.example](file://.env.example)
- [installation.md](file://docs/getting-started/installation.md#environment-configuration)

## 通过Docker部署

Docker 部署是推荐给初学者的方法，因为它简单且易于管理。

### 单容器部署（推荐给初学者）

1. **创建项目目录**
```bash
mkdir open-notebook && cd open-notebook
```

2. **创建 Docker Compose 文件**
```bash
cat > docker-compose.yml << 'EOF'
services:
  open_notebook:
    image: lfnovo/open_notebook:v1-latest-single
    ports:
      - "8502:8502"  # Web UI
      - "5055:5055"  # API
    env_file:
      - ./docker.env
    pull_policy: always
    volumes:
      - ./notebook_data:/app/data
      - ./surreal_single_data:/mydata
    restart: always
EOF
```

3. **创建环境文件**
```bash
cat > docker.env << 'EOF'
# 必需：至少一个 AI 提供商
OPENAI_API_KEY=your-openai-key-here

# 数据库设置（不要更改）
SURREAL_ADDRESS=localhost
SURREAL_PORT=8000
SURREAL_USER=root
SURREAL_PASS=root
SURREAL_NAMESPACE=open_notebook
SURREAL_DATABASE=production

# 可选：密码保护
# OPEN_NOTEBOOK_PASSWORD=your_secure_password
EOF
```

4. **启动 Open Notebook**
```bash
docker compose up -d
```

### Docker 服务管理

```bash
# 检查服务状态
docker compose ps

# 查看日志
docker compose logs -f

# 停止服务
docker compose down

# 更新到最新版本
docker compose pull
docker compose up -d

# 重启特定服务
docker compose restart open_notebook
```

**Section sources**
- [docker-compose.yml](file://setup_guide/docker-compose.yml)
- [docker.env](file://setup_guide/docker.env)
- [installation.md](file://docs/getting-started/installation.md#docker-installation)

## 直接运行部署

如果您是开发者或需要对系统进行定制，可以选择源码安装。

### 先决条件安装

#### macOS
```bash
# 安装 Homebrew（如果尚未安装）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 uv（Python 包管理器）
brew install uv

# 安装 Docker Desktop
brew install --cask docker
```

#### Ubuntu/Debian
```bash
# 更新包列表
sudo apt update

# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# 安装 Docker
sudo apt install -y docker.io docker-compose-plugin
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
```

### 源码安装步骤

1. **克隆仓库**
```bash
git clone https://github.com/lfnovo/open-notebook.git
cd open-notebook
```

2. **配置环境**
```bash
# 复制环境模板
cp .env.example .env

# 编辑环境文件，添加您的 API 密钥
nano .env
```

3. **安装 Python 依赖**
```bash
# 安装所有必需的包
uv sync

# 安装额外的系统特定包
uv pip install python-magic
```

4. **启动所有服务**
```bash
# 一次性启动所有服务
make start-all
```

这将启动：
- SurrealDB 数据库（端口 8000）
- FastAPI 后端（端口 5055）
- 后台工作进程
- React 前端（端口 8502）

**Section sources**
- [Makefile](file://Makefile)
- [installation.md](file://docs/getting-started/installation.md#manual-installation)

## 创建第一个笔记本

现在您已经成功部署了 Open Notebook，让我们创建您的第一个笔记本并添加内容源。

### 步骤 1：创建笔记本

1. 访问 `http://localhost:8502`
2. 点击 "New Notebook"
3. 输入名称，例如 "我的研究项目"
4. 添加详细描述，帮助 AI 理解您的研究目标
5. 点击 "Create Notebook"

### 步骤 2：添加内容源

Open Notebook 支持多种内容源类型：

#### 添加网页链接
1. 点击 "Add Source"
2. 选择 "Link" 类型
3. 粘贴 URL，例如 `https://www.example.com/article`
4. 添加标题并点击 "Add Source"

#### 添加文本内容
1. 点击 "Add Source"
2. 选择 "Text" 类型
3. 直接输入或粘贴文本内容
4. 添加标题并点击 "Add Source"

#### 添加文件
1. 点击 "Add Source"
2. 选择 "File" 类型
3. 上传 PDF、文档或其他支持的文件
4. 系统将自动提取文本内容

### 步骤 3：与 AI 交互

1. 在左侧列中点击一个源
2. 在右侧面板中与 AI 聊天
3. 询问关于您内容的问题
4. 使用 "Save as Note" 按钮保存有用的 AI 响应

**Section sources**
- [first-notebook.md](file://docs/getting-started/first-notebook.md)

## 常见问题排查

### 常见问题及解决方案

| 问题 | 解决方案 |
|------|----------|
| **"无法连接到服务器"** | 设置 `API_URL` 环境变量以匹配您访问服务器的方式 |
| **空白页面或错误** | 确保在 Docker 命令中暴露了 8502 和 5055 两个端口 |
| **在服务器上工作但在其他计算机上不工作** | 不要对远程访问使用 `localhost`，使用服务器的实际 IP 地址 |
| **"404" 或 "config endpoint" 错误** | 不要在 `API_URL` 中添加 `/api`，只需使用 `http://your-ip:5055` |
| **端口已被占用** | 使用 `lsof -i :8502` 找到并停止冲突进程，或使用不同的端口 |

### Docker 权限问题

如果遇到 Docker 权限被拒绝的问题：
```bash
# 将用户添加到 docker 组
sudo usermod -aG docker $USER
# 注销并重新登录
```

### 数据持久化问题

确保卷正确挂载：
```bash
# 检查卷权限
ls -la ./notebook_data
ls -la ./surreal_data

# 如需修复权限
sudo chown -R $USER:$USER ./notebook_data
sudo chown -R $USER:$USER ./surreal_data
```

**Section sources**
- [installation.md](file://docs/getting-started/installation.md#troubleshooting)
- [5-minute-setup.md](file://docs/getting-started/5-minute-setup.md#common-mistakes-to-avoid)

## 验证系统运行

完成安装后，验证系统是否成功运行。

### 服务健康检查

```bash
# 检查所有服务状态
docker compose ps

# 测试数据库连接
curl http://localhost:8000/health

# 测试 API 后端
curl http://localhost:5055/health

# 测试 React 前端
curl http://localhost:8502/healthz
```

### 功能测试

1. **创建测试笔记本**
   - 访问 `http://localhost:8502`
   - 创建一个名为 "测试笔记本" 的新笔记本
   - 添加一个包含 "这是 Open Notebook 安装的测试文档。" 的文本源

2. **测试聊天功能**
   - 转到聊天标签
   - 询问："这个文档是关于什么的？"
   - 您应该收到关于测试文档的响应

3. **测试搜索功能**
   - 添加多个源到您的笔记本
   - 使用搜索栏查找特定内容
   - 验证全文和语义搜索是否正常工作

4. **测试转换功能**
   - 选择一个源
   - 点击 "Transform" → "Summarize"
   - 验证转换是否成功完成

**Section sources**
- [installation.md](file://docs/getting-started/installation.md#verification-and-testing)